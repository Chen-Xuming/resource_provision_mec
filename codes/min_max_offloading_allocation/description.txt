
同时考虑关联关系和资源分配。

关联关系算法：
（1）只考虑传输时延和处理时延的DP算法
（2）就近选择（A和R类型）
（3）随机选择

budget的初始化方法：
（1）假定服务的服务率和单位价格呈正相关
（2）用户的到达率之和为 λ
（3）budget = (λ / max_μ_a * price_a) + (λ / μ_q * price_q) + (λ / max_μ_q * price_q) + k.  k是额外添加的预算

子服务的问题？

